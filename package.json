{
  "name": "pharma-monorepo",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "dev:elmiron": "turbo run dev --filter=elmiron",
    "dev:missed-bladder": "turbo run dev --filter=missed-bladder"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "typescript": "^5.4.0"
  }
}

